// Vuex module with separeted into files state/getters/mutations/actions
import actions from './actions'
import getters from './getters'
import mutations from './mutations'

const store = {
  state: {

  },

  actions,
  getters,
  mutations
}

export default store
